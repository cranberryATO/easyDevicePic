# Absolute site origin, used for <link rel="canonical"> and hreflang tags.
# Google requires fully-qualified URLs there, and keeping them absolute also
# stops Vite rewriting them into hashed asset copies at build time.
# Change this to your real domain before deploying.
VITE_SITE_URL=https://easydevicepic.andretousch.com
